# CONTEXT.md — ITA Lab

תקציר ל-Claude Code (או כל מתכנת אחר) שממשיך את הפרויקט הזה בפעם הבאה.
מילות מפתח: ITA, ITA Lab, routing engine, path engine, data center, MDA,
strands, bulk planning, zones, pathengine.py, bulkplan.py

## מה זה

אב-טיפוס עצמאי (בלי חיבור אמיתי ל-ITA) שמחליף תכנון ידני של חיבורי סיב/נחושת
בין מסדים במנוע שמחשב מסלול חוקי אוטומטית — אוכף סוג כבל תואם, גדיל (strand)
פנוי בכל trunk, ורדונדנטיות A/B בלי חפיפת מקטע פיזי. שלב א' בלבד: סימולציה
מלאה, כל הנתונים סינתטיים (`generate_topology.py`). הפרויקט נבנה כדי להציג
גרסה ראשונית לפני חיבור אמיתי ל-ITA.

**עיקרון תשתיתי שחוזר בכל הקוד**: הכול רץ ברשת סגורה בלי אפשרות להתקין כלום.
`networkx` היא **התלות היחידה**. השרת הוא `http.server` מהספרייה הסטנדרטית;
קבצי xlsx **נקראים וגם נכתבים ידנית** (zipfile + XML) — אין `openpyxl`, אין
`pandas`, אין npm. אם עולה פיתוי להוסיף תלות, זו הסיבה שלא.

## מודל פיזי (גרסה 3 — 4 טורים × 8 פודים + MDA)

- **4 טורים** (A, B, C, D), **8 פודים בכל טור** → 32 פודים. שם פוד =
  `<טור><מספר>` → `A1`, `D5`.
- **2 חדרים**: AB (טורים A,B), CD (טורים C,D).
- **4 פודי MDA ייעודיים, בלי מחשוב כלל**: `A2`, `A6`, `D2`, `D6`
  (הטור החיצוני של כל חדר, עמדות 2 ו-6). שאר 28 פודים = מחשוב רגיל.
- כל פוד (MDA או רגיל) באותה צורה פיזית: 2 שדרות S/N, 10 מסדים בשדרה,
  42U כל מסד. שם מסד: `<פוד>-<שדרה><מספר>` → `D5-N05`. כתובת רכיב/פורט:
  `<פוד>-<שדרה><מספר>-U<UU>-P<PP>` → `D5-N05-U20-P33`.
- EOR/hub בעמדות S01 (דומיין A) ו-N10 (דומיין B) בכל פוד.
- **פוד רגיל**: 9 מסדי מחשוב + 1 EOR בכל שדרה; ה-EOR מתחבר החוצה ל-MDA
  שהוקצה לפוד (לפי קרבה במספר העמדה: 1-4→MDA בעמדה 2, 5-8→MDA בעמדה 6,
  בלי קשר לטור). כל MDA משרת בדיוק 7 פודים.
- **פוד MDA**: S01/N10 הם hub racks גדולים (פאנלים+מתגי core). שאר 18
  העמדות הן רזרבה פיזית, לא מחוברות בגרף.
- Backbone (סיב בלבד): בתוך חדר — שני ה-MDA מחוברים ישירות לפי דומיין
  (`A2-S01<->A6-S01`, `A2-N10<->A6-N10`). בין חדרים — קישור אחד לכל
  דומיין, על hub racks נפרדים לגמרי (`A2-S01<->D2-S01` דומיין A,
  `A6-N10<->D6-N10` דומיין B), עם קיבולת גדולה יותר כי זה צוואר הבקבוק
  היחיד לכל התעבורה הבין-חדרית.
- כל מסד מחשוב מחובר בגישור **לשני** ה-EOR של הפוד שלו (S וגם N) — כדי
  לאפשר זוג A/B בלי מקטעים משותפים.
- Dataset: 640 מסדים, 6,312 רכיבים, 120,256 פורטים, 1,000 חיבורים קיימים.

## מודל ה-trunk: port-to-port, לא מונה קיבולת

זה שינוי מהותי מהגרסאות המוקדמות ו**קל לשבור אותו בטעות בחזרה**. פעם trunk
היה `capacity` + `used`, כלומר מונה. היום כל trunk הוא אוסף של **גדילים
ממוספרים** (strands), וכל חיבור **מחזיק בבעלות** על גדיל ספציפי:

- `strand_port_id(edge_id, cable_type, index, rack)` מייצר מזהה פורט אמיתי
  לכל גדיל: `A1-S05->A1-S01#F008@A1-S05`.
- **זהות הגדיל כוללת את סוג המדיה** — סיב #1 ונחושת #1 של אותו trunk הם
  שני דברים פיזיים שונים ומותר להם להתקיים במקביל. באודיט מוקדם נפלתי
  בדיוק כאן: הבדיקה דיווחה על 436 גדילים "כפולים" והבאג היה בבדיקה, לא
  בנתונים.
- אחסון **דליל** — נשמרים רק הגדילים התפוסים (`strands: {index: circuit_id}`),
  לא מערך באורך הקיבולת.
- `_segment(..., reserved=None)` — כשמחשבים כמה אופציות מסלול במקביל, כל
  אופציה מעבירה `reserved` כדי ששתי אופציות אחיות לא יציעו את אותו גדיל.

## עדיפות קפיצות על אורך

החלטת מוצר מפורשת של המשתמש: **מספר הקפיצות חשוב יותר מאורך הכבל**, כי
האורכים כאן נקבעו רנדומלית ואין להם משמעות אמיתית. המימוש ב-`pathengine.py`:

```python
HOP_COST = 10_000.0        # משקל קשת = HOP_COST + length_m
```

כך שכל קפיצה נוספת עולה יותר מכל הפרש אורך סביר, ו-Dijkstra ממיין קודם לפי
קפיצות ורק אז לפי מטרים. **אם משנים את זה — משנים את סדר העדיפויות של המוצר.**

`INTRA_RACK_M = 3.0` ו-`intra_rack_route()` מטפלים במקרה של שני פורטים באותו
מסד: אפס מקטעים, אפס transit, `domain: "local"`.

## חלוקה לקבוצות צבע (zones)

`zones.py`. הקבוצה היא **גבול קשיח**: רכיב ששויך לקבוצה חייב להיות ממוקם בפוד
של הקבוצה שלו ולא להתערבב עם אחרות.

| קבוצה | פודים |
|---|---|
| ירוק | A3 A4 A5 B3 C3 D3 D4 D5 |
| כחול | A1 B1 B2 C1 C2 D1 |
| לבן | A7 B7 C7 D7 |
| צהוב | כל השאר (A8 B4 B5 B6 B8 C4 C5 C6 C8 D8) — נגזר, לא רשום ידנית |
| **נטרלי** | A2 A6 D2 D6 — פודי ה-MDA **לא שייכים לשום צבע, גם לא לצהוב** |

`resolve()` מקבלת גם עברית וגם אנגלית, מתעלמת מרישיות, וסובלת שגיאות כתיב.
`correction()` מחזירה מה תוקן — **תיקון אף פעם לא שקט**. אם הצבע לא מזוהה
נזרקת `ZoneError` ולא נופלים ל"בכל מקום": גבול קשיח לא מתדרדר לשום גבול
בגלל טעות הקלדה.

## מיקום ציוד חדש (placement.py)

כשגיליון מכריז על רכיב חדש (סריאל, גובה U, כמות פורטים, סוג כבל) ועל מי הוא
מתחבר — הכלי ממליץ **איפה להרכיב אותו**. ציון משולב:

```
SAME_RACK=0  SAME_ROW=200  SAME_POD=260  SAME_MDA=500  SAME_ROOM=700  ELSEWHERE=1200
FULLNESS_WEIGHT=400 (בחזקת 3)     PREFER_BONUS=-10_000
```

- `eligible_racks()` **מוציא לגמרי EOR ו-MDA** — לא מרכיבים שם ציוד (החלטה
  מפורשת של המשתמש).
- `FULLNESS_WEIGHT` בחזקת שלוש: מסד כמעט מלא נדחה חזק, לא לינארית.
- `PREFER_BONUS` הוא -10,000 ולא פחות — ערך חלש יותר לא הצליח לגבור על
  הבונוס של "אותו מסד" ודגש מפורש של המשתמש היה נבלע.

## תכנון מרוכז מאקסל (bulkplan.py)

התרחיש האמיתי: מגיע גיליון עם זוגות פורט-מקור/פורט-יעד, לפעמים גם טאב של
רכיבים חדשים. הזרימה:

```
העלאת xlsx → validate() → תצוגת ביקורת → plan() → תצוגה ויזואלית → execute()
```

**עיקרון שכל העבודה כאן נשענת עליו: הביקורת חייבת לחזות את התוכנית בדיוק.**
מה שהביקורת אישרה — התכנון יתכנן; מה שהביקורת פסלה — התכנון יסרב. זה נאכף
מבנית: `plan_devices()` מריצה בעצמה את `validate_devices()` ומסרבת לכל מה
שהיא פסלה, במקום ששתיהן ישפטו בנפרד ויסטו זו מזו. באודיט אחד בדיוק זה נשבר
(טאב הרכיבים) ותוקן — אם מוסיפים כלל ולידציה חדש, להוסיף אותו במקום אחד.

- **טאב `Devices`** = מפרט בלבד (סריאל, TYPE, LABEL, U, פורטים, ZONE).
- **טאב `P2P`** = קישוריות, כתובה לפי סריאל **כאילו הציוד כבר מותקן**.
- כתובת פורט: `<serial>:<port>`. סריאל בלבד = "הרכיב הזה, כל פורט מתאים".
- שמות עמודות מזוהים דרך טבלאות aliases (`SRC_ALIASES`, `DST_ALIASES`,
  `GROUP_ALIASES`, `CABLE_ALIASES`, `LABEL_ALIASES`, `DEV_*_ALIASES`) —
  כולל עברית (`תיאור`, `שם`, `הערה`, `קבוצה`, `צבע`). `_pick()` מנסה
  התאמה מדויקת קודם ורק אז fuzzy, וכל ה-aliases של אותו שדה ממופים
  לאותו sentinel כדי ששוויון בתוך קבוצה לא ייחשב לדו-משמעות.
- `_resolve_endpoints()` מבדיל בין **פורט שנתפס בשורה קודמת של אותו גיליון**
  (`already claimed by row 7 of this sheet`) לבין **פורט שכבר מחובר במפה**
  (`already patched by CIR-0036`). שתי בעיות שונות עם תיקונים שונים.
- `GROUP` = מזהה שירות/רשת. שורות באותה קבוצה מקובצות יחד ומנוסות
  להישאר בקרבה פיזית (`group_adjacent`).

## סריאלים (serials.py)

10 ספרות, נגזרות ב-SHA-256 מתוך מזהה הרכיב — יציבות בין builds, כך שגיליון
שנכתב בשבוע שעבר עדיין נפתר היום. אומתו: אף port id לא מתפרש כסריאל (0 מתוך
120,256), ואין חפיפה בין סריאלים לשמות רכיבים.

## סבילות לשגיאות כתיב (fuzzy.py)

Damerau-Levenshtein (כולל החלפת אותיות סמוכות — `zoen`/`bleu` הן החלקת אצבע
אחת). תקציב לפי אורך: `n≤3 → 0` (או 1 ב-lenient), `≤5 → 1`, `≤9 → 2`, אחרת 3.
`match()` **מסרבת כשיש תיקו** בין שתי אפשרויות — עדיף לשאול מאשר לנחש.

## עוזר אופליין (assistant.py)

דטרמיניסטי לחלוטין, **בלי שום מודל מאחורה**. המשתמש שקל צ'אטבוט AI פנימי
שקיים ברשת הסגורה שלו, והחליט: *"נשכלל את האופליין כמה שאפשר עד שאשיג יותר
מידע על הצ'אטבוט הפנימי, כרגע נתייחס אליו כאל לא קיים."*

Intents: `clear_constraints`, `avoid`, `prefer`, `list_constraints`,
`why_failed`, `why_placed`, `why_route`, `capacity`, `rack_status`, `locate`,
`help`, `unknown`. הסדר ב-`classify()` הוא **מהספציפי לכללי** ומשנה תוצאות —
הוראה כמו "אל תשתמש ב-D5" מזכירה גם מסד והייתה נקראת כ"איפה D5".

`STOPWORDS` מונע ממה/כמה לחטוף את למה. `unknown` הוא תשובה לגיטימית — בגרסה
מוקדמת `?` הפעיל את ה-help ובלע כל שאלה לא מוכרת, **והבדיקה שלי עברה רק כי
היא השמיטה את סימן השאלה**.

## קבצים

| קובץ | תפקיד |
|---|---|
| `generate_topology.py` | בונה את `data/topology.json` הסינתטי (seed=42, דטרמיניסטי) |
| `pathengine.py` | מנוע הניתוב, גדילים, `commit_route`, `revalidate_route` |
| `bulkplan.py` | תכנון מרוכז מאקסל: קריאה, ולידציה, תכנון, ביצוע |
| `placement.py` | איפה להרכיב רכיב חדש (ציון משולב) |
| `zones.py` | קבוצות הצבע וגבולותיהן |
| `serials.py` | סריאלים מספריים וכתובות `<serial>:<port>` |
| `fuzzy.py` | Damerau-Levenshtein + תקציב לפי אורך |
| `assistant.py` | עוזר אופליין דטרמיניסטי |
| `xlsxreader.py` | קורא xlsx על הספרייה הסטנדרטית בלבד |
| `make_sample_sheet.py` | **כותב** xlsx ידנית (zipfile+XML) — גיליונות לדוגמה |
| `workorder.py` | הוראות עבודה לטכנאי (טקסט) |
| `wo_html.py` | הוראות עבודה מעוצבות להדפסה |
| `server.py` | שרת מקומי + API |
| `ui.html` | ממשק: מפת חווה 4×8 → פוד → מסד → רכיב → פורט, פאנל bulk, צ'אט |
| `cli.py` | גישה משורת פקודה |
| `test_scenarios.py` | **172/172 בדיקות עוברות** |

### API

`/api/topology` · `/api/route` · `/api/circuit` · `/api/execute` ·
`/api/workorder` · `/api/zones` · `/api/assist` · `/api/bulk/plan` ·
`/api/bulk/execute`

`server.py` עוטף כל handler ב-`_guard()` (JSON פגום לא מפיל בקשה) ומשתמש
ב-`ThreadingHTTPServer` + `WRITE_LOCK` לרצפי read-modify-write. נבדק:
9 בקשות מקבילות (plan/assist/zones), אפס שגיאות, plan_id ייחודי לכל אחת.

## מלכודות שכבר נפלתי בהן — לא ליפול שוב

1. **`shortest_simple_paths` הוא גנרטור.** ה-`try` חייב לעטוף את **האיטרציה**,
   לא את הקריאה — אחרת `NetworkXNoPath` בורחת החוצה.
2. **מספרי שורות ב-xlsx** — Excel **משמיט** שורות ריקות. חייבים לקרוא את
   התכונה `r` ולא לספור לפי מיקום.
3. **`Content_Types` ב-OOXML** — צריך override לכל part, אחרת Excel מכריז
   על הקובץ כפגום.
4. **`u_start` הוא ה-U העליון** של הרכיב, והוא גדל כלפי מטה.
5. **בדיקות שעוברות רק על מפה בתולית הן באג בבדיקות.** הסוויטה נהגה לקודד
   קשיח `A1-S05:TOR-SW-01` ולהניח שיש בו פורטים פנויים — אחרי ביצוע אמיתי
   היא נכשלה, וזה נראה כמו באג במתכנן במקום כמו fixture שנגמר. עכשיו היא
   מחפשת רכיב במצב שהיא צריכה. **כל fixture חדש צריך לחפש, לא לקודד.**
6. **RTL/LTR** — שורות של מזהי פורט ב-UI צריכות `direction:ltr` בתוך ממשק
   עברי, אחרת המזהה מוצג הפוך.
7. **התנגשות מזהי רכיבים** — `NEW-{serial[3:9]}` גרם לשני רכיבים שונים
   לקבל אותו id. משתמשים בסריאל המלא + מונה הגנה.

## היסטוריית שינויים (תמצית)

גרסה 3 הביאה את פריסת 4×8+MDA, את ה-drill-down במפה, ביצוע מסלול
(`Execute` → `commit_route` → כתיבה ל-`topology.json`), דיאגרמת "קפיצות כבל"
ב-SVG, Work Order שמסמן `EXECUTED — committed as CIR-XXXX`, וליטוש UI לקראת
דמו-אישור. מאז נוספו: מודל הגדילים, שתי אופציות מסלול מחושבות + "עוד
אפשרויות" (במקום להסתמך על תיוג A/B, כי **השמות בשטח לא עקביים** — לפעמים
A/B, לפעמים 1/2, לפעמים FIRST/SEC), תכנון מרוכז מאקסל עם ביקורת מקדימה,
מיקום ציוד חדש, קבוצות צבע, סריאלים מספריים, סבילות לשגיאות כתיב, עוזר
אופליין, ותצוגה ויזואלית לכל שורה מתוכננת.

## מה עוד לא נעשה (TODO לפעם הבאה)

1. **חיבור אמיתי ל-API של ITA** — רק `load_topology()`/`commit_route()`
   ב-`pathengine.py` צריכים implementation חדש. זו המטרה המרכזית, בכפוף
   לאישור מהדמו.
2. **`decommission_route()`** — כרגע אפשר רק לחבר, לא לשחרר. הגדילים
   והפורטים נשארים תפוסים לנצח.
3. **תיבת חיפוש לפי סריאל/פורט** בממשק.
4. **דוח קיבולת** — אילו trunkים מתקרבים למלא.
5. **גישור מעבר ב-EOR/hub כפורט אחד**, לא זוג כניסה/יציאה.
6. **18 העמדות הרזרבה בכל פוד MDA** לא מחוברות בגרף הניתוב.
7. אין עדיין סוגי fiber (SM/MM), אילוצי חשמל/קירור, סוגי device נוספים.

## סטטוס נכון ל-2026-08-26

- Git repo, remote: `github.com/yoavlevenberg/ita-lab.git`, ענף **master**
  (המשתמש ביקש לעבוד ישירות על master, בלי ענפי פיצ'ר).
- **הכל דחוף ל-GitHub**, מקומי ו-remote מסונכרנים. commit אחרון:
  `71e42cb` — אודיט שלישי (הפרדת הודעות התנגשות פורט, חיזוק הסוויטה).
- **המשתמש עובד ממספר מכשירים** — תמיד `git pull` (לא רק `git status`)
  בתחילת סשן, במיוחד לפני עריכת `data/topology.json` או קבצי קוד.
- `data/topology.json` **מנוהל ב-git** והוא כרגע במצב בתולי (1,000 חיבורים,
  CIR-0001..CIR-1000). `generate_topology.py` דטרמיניסטי (seed=42) ומייצר
  בדיוק את הקובץ הזה — כך שהרצה שלו על מצב בתולי היא no-op. **אבל אם בוצעו
  חיבורים חדשים והם לא נדחפו — הרצה שלו תמחק אותם.**
- `data/sample_demand*.xlsx` **לא ב-git** (ב-`.gitignore`) — מייצרים מחדש
  עם `make_sample_sheet.py`, כי מזהי הפורטים מתיישנים ברגע שמבצעים תוכנית.
- כלי צילום המסך של הדפדפן (claude-in-chrome) **נכשל ב-timeout שוב ושוב**
  בסשנים האחרונים. אימות UI נעשה ישירות מול השרת (fetch ל-API + בדיקת
  ה-HTML המוגש) ולא דרך צילומי מסך.
