# CONTEXT.md — ITA Lab

תקציר ל-Claude Code (או כל מתכנת אחר) שממשיך את הפרויקט הזה בפעם הבאה.
מילות מפתח: ITA, ITA Lab, routing engine, path engine, data center, pathengine.py

## מה זה

אב-טיפוס עצמאי (בלי חיבור אמיתי ל-ITA) שמחליף תכנון ידני של חיבורי סיב/נחושת
בין מסדים במנוע שמחשב מסלול חוקי אוטומטית — אוכף סוג כבל תואם, קיבולת פנויה
בכל trunk, ורדונדנטיות A/B בלי חפיפת מקטע פיזי. שלב א' בלבד: סימולציה מלאה,
כל הנתונים סינתטיים (`generate_topology.py`).

## מודל פיזי

- 2 פודים (A, B), בכל פוד 2 שדרות (S, N) × 10 עמדות מסד.
- EOR אחד לכל שדרה, בקצוות מנוגדים: S01, N10. כלומר 9 מסדי מחשוב + 1 EOR
  בכל שדרה.
- כל מסד 42U עם רכיבים אמיתיים במיקומי U. הפורטים שייכים לרכיב, לא למסד.
- Dataset: 410 devices, 7212 ports, 260 circuits קיימים (נוצרו ע"י המנוע
  עצמו דרך ניתוב אמיתי, כדי שהנתונים יהיו עקביים תמיד).
- דומיין A = עובר דרך ה-EOR של שדרה S. דומיין B = עובר דרך ה-EOR של שדרה N.
  כל מסד מחשוב מחובר לשני ה-EOR (הנחה מתועדת, ניתנת לביטול ב-`build_edges()`).
- בין פודים: רק EOR-to-EOR, סיב בלבד, לפי דומיין (`A-S01<->B-S01`,
  `A-N10<->B-N10`). אין נחושת בין פודים בכוונה.

## קבצים

| קובץ | תפקיד |
|---|---|
| `generate_topology.py` | בונה את `data/topology.json` הסינתטי |
| `pathengine.py` | מנוע הניתוב + כל האילוצים + `commit_route` |
| `workorder.py` | הפקת הוראות עבודה לטכנאי |
| `server.py` | שרת מקומי (stdlib בלבד, בלי Flask/npm) + API |
| `ui.html` | ממשק גרפי דמוי-ITA |
| `cli.py` | גישה משורת פקודה |
| `test_scenarios.py` | 19/19 בדיקות עוברות |

## תיקון מרכזי שבוצע

`resolve_path()` ב-`pathengine.py` בודק **את שני הדומיינים A ו-B** ובוחר לפי
`(אורך כולל, -קיבולת פנויה הכי צרה)` — במקום להחזיר את הדומיין הראשון
שעובד. בלי זה המנוע היה מעמיס הכל על דומיין A ומשאיר את B ריק.

## מה עוד לא נעשה (TODO לפעם הבאה)

1. **חיבור אמיתי ל-API של ITA.** צריך implementation חדש רק לשתי פונקציות
   ב-`pathengine.py`: `load_topology()` (קריאה) ו-`commit_route()` (כתיבה —
   כבר בנויה בדיוק בצורה שתשמש לכתיבה חזרה). כל לוגיקת האילוצים נשארת כמות
   שהיא.
2. **אין כתיבה אמיתית עדיין** — כל Work Order מסתיים ב-
   `STATUS: PROPOSED — Requires network engineer approval`. בכוונה: כתיבה
   חוזרת רק אחרי אישור מהנדס.
3. **גישור מעבר ב-EOR מיוצג כפורט אחד**, לא כזוג כניסה/יציאה. פישוט מודע.
4. **Work Order הוא טקסט**, לא מסמך Word מעוצב — לשדרג אחרי שהלוגיקה מאושרת.
5. **אין עדיין**: תמיכה בסוגי fiber (SM/MM), אילוצי חשמל/קירור, סוגי device
   נוספים.

## סטטוס נכון ל-2026-08-19

- Git repo, remote: `github.com/yoavlevenberg/ita-lab.git`.
- קומיט יחיד עד כה: "Initial commit: ITA Lab v2 with routing engine, tests,
  and GUI".
- ייתכן שינוי לא-committed ב-`data/topology.json` (מהרצה מקומית של
  `generate_topology.py`) — לבדוק `git status` בהתחלת הסשן.
